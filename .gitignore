# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# RotaViva user-registered maps (runtime data; presets are tracked separately)
backend/data/maps/*.json
# RotaViva optimization-run database (runtime data)
backend/data/*.db

# Node / frontend
node_modules/
frontend/dist/
*.local

# Editor / OS
.DS_Store

# brainstorming visual companion (local, ephemeral)
.superpowers/

# planning/spec working docs (local-only; purged from history later)
docs/superpowers/

# local design/handover docs (kept out of version control)
HANDOVER.md
design.md
